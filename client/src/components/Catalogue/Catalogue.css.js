import { fade, makeStyles } from '@material-ui/core/styles';


export const catalogueStyles = makeStyles((theme) => ({
    catalogueConteiner: {
       maxWidth: '50em',
       minWidth: '50em'

    },
    cardContent: {
        textAlign: "center",
    }
    
   
  }));